class Grid{
    constructor(width, height, scale) {
        this.rows = width/scale
        this.columns = height/scale
        this.scale = scale
        this.size = (self.rows, self.columns)

    }
}